# Raspberry Pi Client Environment Configuration
# Copy this file to .env.rpi and adjust values for your setup

# === Server Connection ===
# IP address of your Mac server running the AI assistant
SERVER_IP=192.168.1.100

# === Audio Devices ===
# USB Microphone device (find with: arecord -l)
MIC_DEVICE=hw:1,0

# Bluetooth Speaker device (usually "bluealsa" after bt-audio-start.sh)
SPEAKER_DEVICE=bluealsa

# === Audio Parameters ===
# Sample rate for recording (16kHz is optimal for Whisper STT)
SAMPLE_RATE=16000

# Audio chunk size in bytes
CHUNK_SIZE=1024

# === GPIO Configuration ===
# GPIO pin number for record button (BCM numbering)
# Default: GPIO 17 (Physical Pin 11)
GPIO_BUTTON_PIN=17

# Button debounce time in milliseconds
GPIO_DEBOUNCE_MS=50

# === Logging ===
# Log level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

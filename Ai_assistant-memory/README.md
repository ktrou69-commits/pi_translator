# 🤖 AI Bro with Memory

Чат-бот "Кент", который запоминает факты о вас.
Использует **два ИИ** (Gemini):
1.  **Собеседник:** Веселый, общается на сленге, использует память.
2.  **Архивариус:** Анализирует ваши сообщения и сохраняет факты в `memory.json`.

## 🚀 Установка

1.  **Перейдите в папку:**
    ```bash
    cd Ai_assistant-memory
    ```

2.  **Установите библиотеки:**
    ```bash
    pip install -r requirements.txt
    ```

3.  **Ключ API:**
    Ключ уже прописан в файле `.env`.

## ▶️ Запуск

```bash
python ai_assistant.py
```

## 🧠 Как работает память?

Просто общайтесь!
*   Скажите: *"Я купил кота, назвал Барсик"*
*   ИИ ответит.
*   А в консоли вы увидите: `🧠 [Memory AI]: Запомнил -> Купил кота, назвал Барсик`
*   В следующий раз спросите: *"Как зовут моего кота?"* — и он ответит!

Факты хранятся в файле `memory.json`. Вы можете редактировать его вручную, если нужно.

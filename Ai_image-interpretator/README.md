# 👁️ AI Image Interpreter

Умная камера с памятью.
*   **Фото:** Делает снимок по нажатию Enter (OpenCV).
*   **Голос:** Слушает вопрос (Google STT).
*   **Мозг:** Gemini 2.5 Flash анализирует фото + голос.
*   **Память:** Сохраняет увиденное в ОБЩУЮ базу знаний (`../Ai_assistant-memory-voice/memory.json`).

## 🛠 Установка (Raspberry Pi)

1.  **Системные библиотеки:**
    ```bash
    sudo apt-get update
    sudo apt-get install -y python3-opencv libopencv-dev
    sudo apt-get install -y python3-pyaudio portaudio19-dev mpg123 alsa-utils flac sox libsox-fmt-all
    ```

2.  **Python библиотеки:**
    ```bash
    cd Ai_image-interpretator
    pip install -r requirements.txt
    ```

## 🛠 Установка (macOS)
```bash
brew install opencv portaudio
pip install -r requirements.txt
```

## ⚙️ Настройка (Опционально)

Если у вас несколько камер или микрофонов, настройте их в `.env`:

```bash
# Индекс микрофона (если не слышит)
MIC_INDEX=0

# Индекс камеры (если не видит или не та камера)
CAMERA_INDEX=0
```

## ▶️ Запуск
```bash
python image_interpreter.py
```

1.  Нажмите **Enter**.
2.  Скрипт сделает фото.
3.  Задайте вопрос (например: "Как тебе мои новые кроссы?").
4.  ИИ ответит и **запомнит** этот факт навсегда.

# 🌍 Free Pi Translator

Простой, бесплатный и стабильный переводчик для Raspberry Pi.
Использует Google Translate через библиотеку `deep-translator`.
**Не требует API ключей и регистрации.**

## 🚀 Установка

1.  **Перейдите в папку проекта:**
    ```bash
    cd pi_translator
    ```

2.  **Установите зависимости:**
    ```bash
    pip install -r requirements.txt
    ```
    *(Если возникают ошибки прав доступа, используйте `pip3 install -r requirements.txt --break-system-packages` или виртуальное окружение)*

## ▶️ Запуск

```bash
python translator.py
```

## 💡 Использование

*   Просто вводите текст (на русском или любом другом).
*   Программа переведет его на английский.
*   Чтобы выйти, введите `exit` или нажмите `Ctrl+C`.

## 🎧 Версия с озвучкой (для наушников)

Если у вас есть наушники (Bluetooth или проводные), используйте скрипт `translator_tts.py`:

```bash
python translator_tts.py
```
Он работает так же, но **озвучивает** перевод на английском языке.
Для работы нужно установить плеер и утилиты: `sudo apt-get install mpg123 alsa-utils`.

## 🛠 Требования
*   Python 3
*   Интернет соединение (Wi-Fi)

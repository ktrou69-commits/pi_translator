# 🎙️ Voice Translator for Raspberry Pi

Голосовой переводчик: **Голос (RU) -> Текст -> Перевод (EN) -> Озвучка (EN)**.
Работает онлайн через Google сервисы.

## 🛠 Предварительная настройка (System Dependencies)

На Raspberry Pi нужно установить системные библиотеки для работы со звуком:

```bash
sudo apt-get update
sudo apt-get install -y python3-pyaudio portaudio19-dev mpg123 flac
```
*   `python3-pyaudio` / `portaudio19-dev`: Драйверы для микрофона.
*   `mpg123`: Легкий плеер для воспроизведения MP3 (озвучки).
*   `flac`: Кодек, иногда нужен для распознавания речи.

## 🚀 Установка проекта

1.  **Перейдите в папку:**
    ```bash
    cd pi_translator_stt-tts
    ```

2.  **Установите Python библиотеки:**
    ```bash
    pip install -r requirements.txt
    ```

## 🎤 Настройка микрофона

Убедитесь, что USB-микрофон подключен. Проверить можно командой:
```bash
arecord -l
```
Если устройство видно, всё должно заработать автоматически.

## ▶️ Запуск

```bash
python voice_translator.py
```

## 💡 Использование

1.  Дождитесь надписи `✅ Ready! Speak now.`
2.  Скажите фразу на русском.
3.  Программа распознает её, переведет и озвучит на английском.
4.  Для выхода нажмите `Ctrl+C`.

## ⚙️ Смена языков

Чтобы поменять язык (например, на Немецкий), откройте `voice_translator.py` и измените переменные в начале `main()`:

```python
target_lang_code = 'de' # German for translation
tts_lang = 'de'         # German for TTS
```
